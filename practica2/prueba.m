#!/usr/local/bin/octave -qfif (nargin != 1)    printf("Usage: train_model.m <data_filename> <training_partition>");endarglist = argv();datafile = arglist{1};disp("Loading data...");load(datafile);disp("Data load complete");[te, tr] = shuffle(data, 0.9);[ps0, ps, ph0, ph] = train_model(tr);disp(clasifier_for(ps0,ps,ph0,ph,te));